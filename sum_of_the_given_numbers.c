#include<stdio.h>
int main()
{
    int i,bittu;
    scanf("%d",&bittu);
    for(i=1;i<=bittu;i++)
    {
        int a,b;
        scanf("%d%d",&a,&b);
        int c;
        c=a+b;
        printf("%d
",c);
    }
}